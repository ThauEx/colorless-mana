{% extends 'base.html.twig' %}

{% block title %}{{ 'cards.search.title'|trans }}{% endblock %}

{% block body %}
    <div class="accordion">
        <div class="accordion-item">
            <h2 class="accordion-header">
                <span class="accordion-button">{{ 'cards.search.scryfall_id'|trans }}</span>
            </h2>
            <div class="accordion-collapse collapse show">
                <div class="accordion-body">
                    {{ form_start(scryfallForm) }}
                        <div class="input-group">
                            {{ form_widget(scryfallForm.scryfall_id) }}
                            {{ form_widget(scryfallForm.submit) }}
                        </div>
                        {{ form_errors(scryfallForm.scryfall_id) }}
                    {{ form_end(scryfallForm) }}
                </div>
            </div>
        </div>
        <div class="accordion-item">
            <h2 class="accordion-header">
                <span class="accordion-button">{{ 'cards.search.set_code_number'|trans }}</span>
            </h2>
            <div class="accordion-collapse collapse show">
                <div class="accordion-body">
                    {{ form_start(setNumberForm) }}
                        <div class="input-group">
                            {{ form_widget(setNumberForm.setCode) }}
                            {{ form_widget(setNumberForm.number) }}
                            {{ form_widget(setNumberForm.submit) }}
                        </div>
                        {{ form_errors(setNumberForm.setCode) }}
                        {{ form_errors(setNumberForm.number) }}
                    {{ form_end(setNumberForm) }}
                </div>
            </div>
        </div>
        <div class="accordion-item">
            <h2 class="accordion-header">
                <span class="accordion-button">{{ 'cards.search.name'|trans }}</span>
            </h2>
            <div class="accordion-collapse collapse show">
                <div class="accordion-body">
                    {{ form_start(nameForm) }}
                        <div class="input-group">
                            {{ form_widget(nameForm.name) }}
                            {{ form_widget(nameForm.submit) }}
                        </div>
                        {{ form_errors(nameForm.name) }}
                    {{ form_end(nameForm) }}
                </div>
            </div>
        </div>
    </div>


    {% if collection is not empty %}
        <hr>

        {% for row in collection|batch(4) %}
            <div class="row">
                {% for item in row %}
                    <div class="col-md-3 mb-3" data-id="{{ item.id }}">
                        <div class="card">
                            <img src="https://api.scryfall.com/cards/{{ item.setCode }}/{{ item.number }}?format=image&version=normal" class="card-img-top img-fluid" alt="">
                            <div class="card-body">
                                <p class="text-center">
                                    {{ item.setCode|set_name }} ({{ item.setCode }}) {{ item.number }}
                                </p>
                                {% set finishes = item.finishes is not empty ? item.finishes : ['nonfoil', 'foil'] %}
                                {% for lang in item.cardLanguages %}
                                    <form action="{{ path('collection_add') }}" method="get">
                                        <div class="text-center">
                                            <label for="{{ item.id }}_{{ lang }}">{{ item.texts(lang).name }} ({{ lang }})</label>
                                        </div>
                                        <input type="hidden" name="setCode" value="{{ item.setCode }}">
                                        <input type="hidden" name="number" value="{{ item.number }}">
                                        <input type="hidden" name="lang" value="{{ lang }}">

                                        {% for finish in finishes %}
                                            <div class="input-group mb-1">
                                                <span class="input-group-text w-50">{{ ('card.finish.' ~ finish)|trans }}</span>
                                                <input type="number" name="amounts[{{ finish }}]" class="form-control" min="0" value="0" id="{{ item.id }}_{{ lang }}_{{ finish }}">
                                            </div>
                                        {% endfor %}
                                        <div class="d-grid mb-3">
                                            <button type="submit" class="btn btn-primary" title="Hinzufügen">
                                                <svg class="bi d-block mx-auto mb-1" width="24" height="24"><use xlink:href="#journal-plus"></use></svg>
                                            </button>
                                        </div>
                                    </form>
                                {% endfor %}
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% endfor %}
    {% endif %}
{% endblock %}
